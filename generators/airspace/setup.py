import setuptools

setuptools.setup(
        name="punch-demo",
    version="1.0.0",
    keywords=["punch"],
    install_requires=[
    "numpy==1.19.4"
    ],    
    packages=setuptools.find_packages(),
)

